export default function TestBotPage() {
  return (
    <div className="p-8 text-center text-sm text-gray-400">
      <p>This temporary placeholder route exists to clear a lingering build reference. You can remove <code>app/test-bot</code> after a successful build.</p>
    </div>
  );
}
